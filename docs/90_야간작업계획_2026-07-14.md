# 야간 자율 작업 계획 (10시간)

작성: 2026-07-13 심야, 목표 완료: 익일 아침

## 원칙 (막히면 뚫고, 절대 멈추지 않는다)

1. **한 작업이 막히면 30분 안에 판단하고 다음으로 넘어간다.** 막힌 이유는 반드시 기록한다.
2. **"데이터가 없어서 못 한다"는 답을 금지한다.** 대신 ①다른 경로를 찾거나 ②합성 데이터로
   로직을 완성해 두거나 ③"무엇이 오면 켜지는지"를 코드, 문서로 남긴다.
3. **모든 발견은 시험으로 고정한다.** 시행착오는 주석에 남겨 같은 실수를 반복하지 않는다.
4. **기준값 불변**: 번호방 111 (3F 51 / 4F 60), LBL 3/17. 매 작업 후 확인.
5. **게이트를 함부로 열지 않는다.** unreviewed 규칙은 미리보기(dry-run)로만 본다.
6. 커밋은 작업 단위로. author=geonumul. 브랜치 `feat/pressure-regime-boundaries`.

---

## 방금 확인한 사실 (계획의 출발점)

기준 시설(내용고형제) 평면도의 **벽 레이어를 찾았다**:

| 레이어 | 개수 | x 범위 |
|---|---|---|
| `크린판넬` | 1,487 | 97,179 ~ 473,699 |
| `wall` | 324 | 17,149 ~ 221,899 |
| `판넬` | 129 | 394,199 ~ 652,399 |
| `스테인레스 칸막이벽` | 16 | - |
| `DOOR` (문) | 496 | 21,034 ~ 846,163 |
| **방 라벨** | 960 | 17,765 ~ 868,217 |

**예전에 "좌표계가 뒤섞였다"고 본 것은 오해였을 가능성이 크다.**
한 DXF 에 **여러 층 평면이 나란히** 배치돼 있고, 층마다 다른 판넬 레이어를 썼을 뿐이다
(x 범위가 겹치지 않고 이어진다). 새 참고도면도 6장이 나란히 있었다 - 같은 패턴이다.
→ **벽 레이어를 전부 합치면** flood-fill 이 될 것이다. 이게 오늘밤 1번 작업.

---

## 작업 순서 (막히면 다음으로)

### A. 기준 시설 방 경계 (예상 2h) 최우선
- A1. 벽 레이어 전부 합쳐 flood-fill. 층별로 나뉘는지 확인(x 범위로 시트 분리)
- A2. 프로파일 `osd_hs_2025.yaml` 에 `boundaries` 절 추가
- A3. 111방 중 몇 개나 경계가 잡히는지 측정. **그림으로 눈 검증**
- A4. 인접을 polygon 으로 승격 → 근사 175쌍이 몇 쌍으로 바뀌는지 비교
- **막히면**: 층별 분리가 안 되면 층 단위로 따로 돌린다. 그래도 안 되면 B 로 넘어간다.

### B. 새 참고도면을 시설로 등록 (예상 2h)
- B1. 한 DXF 에 6장이 든 구조를 다루는 방법 결정
      (시트별 x 오프셋으로 자르거나, 프로파일에 sheet_origin 개념 추가)
- B2. `참고도면_2f` 프로파일 작성 (레이어: Grade / RoomName / ROOMNUMBER / 차압 / Air Flow *)
- B3. facility add → ingest → **등급 49, 절대압력 45, 화살표 39 가 실제로 적재되는지**
- B4. `preview_rules.py` 로 **규칙 4개 전부** 실검증 (PRES-001/002/003, ADJ-001)
- **막히면**: 6장 분리가 어려우면 차압 흐름도 시트 하나만 잘라 별도 DXF 로 만들어 등록한다.

### C. 규칙 정밀화 (예상 2h)
- C1. **`칭량 전 원료대기실` 오탐 해결** - '대기실, 전실, 보관' 이 붙으면 분진 공정이 아니다
      (원료가 아직 밀봉돼 있다). 부정 낱말(negation) 개념 도입
- C2. ADJ-002(갱의 체인) 구현 - 이제 등급, 인접이 있으니 가능
      근거: 고시 별표1 제4호 타목(동선 D→C→B 점진), Q&A 문16(전실은 높은 쪽 등급)
- C3. 새 규칙 후보 (10_법규/조문근거_색인.md H, H-2절에서):
      - 통과동선 금지 (별표17 3.2.1마) - 작업실이 통로가 되는가
      - 화장실이 청정지역 내에 있는가 (2010 안내서 p.8)
      - 무균시험실 전실(前室) 존재 (시행규칙 제2조⑥)
      - 보관소 구획 (원료, 자재, 제품, 부적합, 반품)
- **막히면**: 구현이 막히는 규칙은 순수 로직 + 합성 시험만 만들고 게이트로 잠가 둔다.

### D. 리포트, 검수 자료 (예상 1.5h)
- D1. HTML 리포트에 **regime, 면적, 경계** 표시
- D2. **대표님 검수 요청서** 자동 생성:
      - 봉쇄실 31개 목록 (이름, 번호, 판정근거) → "이 분류가 맞습니까?"
      - PRES-003 검출 건 → "이게 실제 위반입니까, 설계 의도입니까?"
      - 사내 차압 기준서 요청
- D3. 발주처 프로파일 질문지 (층수, 연면적, 스프링클러, 내화, 소재지, 용제, 주성분)

### E. 마무리 (예상 1h)
- E1. 전체 시험 통과 확인 + 기준값 불변 확인
- E2. 01_진행상황.md 갱신, 커밋
- E3. **아침 보고서** - 무엇을 했고, 무엇이 막혔고, 무엇을 물어야 하는지

### F. 남는 시간이 있으면 (여유분 1.5h)  - **여기서 가장 큰 게 나왔다**

- [x] **나머지 시트를 열었다.** 6장 중 **3장을 통째로 못 보고 있었다.**
      `0 천정기구(HEPA, 풍량121)`, `3 차압계배치(28)`, `5 인터락(24)`
      → 시트마다 x 원점이 다르다. **기준 시트 좌표계로 평행이동**해서 잘라 온다
        (scripts/2_시트전처리/cut_ref_sheet_aligned.py)
- [x] **PRES-005 차압계** (별표1 4-너). 범위를 좁혀 **거짓 위반 6건을 막았다**
      (차압계 없는 6구간이 전부 CNC↔NC - 조문은 '**청정실**과 주변구역 사이'라 한다)
- [x] **ADJ-005 인터락** (별표1 4-파). **구판을 인용했으면 못 만들 규칙이었다**
      (구판: "인터락 **또는** 경보장치" / 현행: A, B는 **인터락 필수**, C, D는 시각경고)
      → 갱의 체인에 인터락이 하나도 없다. **위반 후보 2건**
- [x] **시험이 교차 버그를 잡았다**: `무균 **충전실**` 이 '전실'을 품어 **에어락으로 오인**.
      세 규칙(ADJ-001/002/005)에 다 퍼져 있었다. 오인하면 **진짜 위반을 놓친다**
- [x] **천정 시트의 `풍량` 118개 적재.** 단위는 **도면이 알려줬다**(`B-ZONE` 의 '풍량(CMH)')
      → TA 단위 보류 하나가 풀렸다. 교차검증도 됨(2.7m 가정 시 B 63, C 53, D 28회/hr - 상식과 일치)
- [x] **환기 횟수(ACH) 규칙은 만들지 않는다.** 수치는 **구 KGMP 해설서**에만 있고
      **현행 고시엔 없다**(별표1 '환기' 6건 전수 확인 - 전부 EO 멸균 등 다른 맥락).
      근거 없는 관행을 법으로 착각하면 안 된다. **시험이 이 결정을 강제한다**
      (규칙 파일에 '환기횟수'가 생기면 시험이 깨진다)
- [x] **리포트 배너가 거짓말을 하고 있었다** - "XREF 미수령, Grade 미확보, 화살표 미확정"이
      셋 다 해결된 뒤에도 그대로였다. 시설별 실제 상태를 말하도록 고쳤다
- F1. 나머지 5장(천정기구, 리턴풍도, 차압계, 온습도계, 인터락) 추출기
      → HEPA/디퓨저 개수, 리턴 루버, 차압계 위치, 인터락 쌍
- F2. 2010 안내서에서 나온 검증 규칙 더 구현
- F3. 기존 데이터 재감사 (X-1~X-41 교차검증 항목 중 미실행분)

---

## A 결과 - **부분 성공. 막혔고, 이유를 안다.** (2026-07-14 01:xx)

### 얻은 것 (전부 사실로 확정)
1. **예전 "좌표계가 뒤섞였다"는 결론은 틀렸다.**
   한 DXF 에 **여러 시트가 나란히** 배치돼 있을 뿐이다(3F x392k~485k, 4F x521k~611k).
   새 참고도면(6장 나란히)과 **같은 패턴**이다.
2. **벽 레이어를 이름으로 짐작하면 안 된다.**
   `크린판넬`, `판넬`, `wall` 은 **방 라벨이 없는 다른 시트**의 것이었다.
   그걸로 돌렸더니 96개 방이 **전부 하나의 덩어리(40,710㎡)** 로 뭉쳤다 - 벽이 하나도 없었으니까.
   방 라벨이 있는 시트의 벽은 **`하니컴패널`**(허니컴 = 클린룸 벽체). 3F 614개, 4F 606개.
   → **"라벨이 있는 구역 안에서 레이어를 세어라."** (`scripts/5_도면조사/probe_osd_walls2.py`)
3. 그 레이어로 바꾸니 **0 → 35개**가 잡혔다.

### 막힌 것 (정직하게)
`하니컴패널` 만으로는 **방이 안 닫힌다.** close_gap 을 1200mm 까지 올려도:

| close_gap | 방 | 3F | 4F | 인접 |
|---|---|---|---|---|
| 200 | 35/96 | 0/38 | 35/58 | 37 |
| 1000 | 43/96 | 7/38 | 36/58 | 43 |
| 1200 | 45/96 | 9/38 | 36/58 | 46 |

**3F 가 특히 나쁘다(9/38).** 원인 후보:
- **문짝이 안 그려져 있다** - `DOOR` 레이어가 3F 구역에 **21개뿐**이다(개구부가 뻥 뚫림)
- 벽이 여러 레이어에 파편화돼 있을 가능성 (`CEN1`, `251`, `FIN`, `INS`, `0` 등)
  → 레이어 조합 스윕을 시도했으나 **계산이 10분을 넘겨 타임아웃**. 격자가 크고 후보가 많다.

### 다음에 뚫을 방법 (우선순위)
1. **개구부 봉합**: `DOOR` 블록(INSERT)의 삽입점, 회전으로 **문 위치를 알아내 그 자리에 장벽을 그린다.**
   벽 선이 없어도 문이 어디 있는지는 블록으로 알 수 있다. ← **가장 유망**
2. **격자 최적화**: 시트별로 잘라서 돌린다(격자 크기 1/6). 스윕이 가능해진다.
3. **DWG 원본 확보**: 새 참고도면처럼 DWG 를 받으면 블록 구조가 살아 있어 벽이 온전할 수 있다.
   (현재 DXF 는 변환본이라 정보가 깎였을 가능성)
4. 발주처에 **XREF 원본** 요청 (기존 요청 A-2)

**→ B(새 참고도면 등록)를 먼저 하고, 거기서 검증된 방법을 갖고 A 로 돌아온다.**
   새 도면은 51/51 이 이미 성공했으므로, 규칙 검증은 그쪽에서 하는 게 빠르다.

## 진행 기록 (작업하며 갱신)

- [x] A1 벽 레이어 찾기 → **`하니컴패널`** (`크린판넬` 아님). 0 → 35개
- [x] A2 프로파일 boundaries 절 - **적재 완료** (보류 해제)
- [x] A3 경계 검증(그림) → 3F 가 안 닫히는 것을 눈으로 확인
- [x] A4 polygon 인접 승격 - **완료.** 인접 138쌍(기준), 42쌍(참고). 근사 아닌 **진짜 벽 맞댐**
- [x] **A 재개통 (2026-07-14)** - 45 → **79/94** (3F 27/37, 4F 52/57)
      - "벽이 안 그려져 있다"는 **오진**이었다. `_iter_wall_segments` 가 `doc.modelspace()` 만 훑고
        **블록 안으로 안 들어갔다.** 텍스트는 이미 재귀로 고쳤는데 벽은 안 고쳐 둔 채였다
      - 문 막기(+3방), `B-WAL` 레이어 추가(+3방)로 39/94 까지 겨우 올리다가, 재귀 한 줄로 79
      - 참고도면이 51 → 2 로 깨졌다: 51/51 을 낸 스크립트는 **레이어를 안 가리고** 전부 태웠는데
        (블록 안이 전부 레이어 '0' 이라 레이어로 못 고른다) 프로파일이 그걸 못 물려받았다
        → `wall_layers: ["*"]` 지원. 51/51 복구
      - **교훈: CAD 에서 "없다"고 결론 내리기 전에 블록 안을 봤는지 먼저 확인하라**
- [x] B1~B4 새 도면 등록, 규칙 실검증 - **완료.** 방 53(등급 49, 압력 44), 화살표 39(귀속 31), 인접 101
      - **화살표를 거꾸로 읽고 있었다.** 절대압력과 교차대조해서 잡았다(모순 9건 → 1건)
        - 원인1: "화살촉은 -y" 라는 단일 가정. 실제론 블록마다 ±x 로 **정반대**, 일부는 거울반사(xscale<0)
        - 원인2: 방을 붙일 때 **측면거리를 무시**. 축거리 4mm 차로 축에서 3m 벗어난 방을 골랐다
        - → 블록 외곽선에서 화살촉을 **잰다**(arrowgeom.py). matrix44 로 회전, 거울반사 처리
      - **도면이 구간별 목표 차압을 적어뒀다** - 화살표 레이어 이름이 `Air Flow 10Pa`/`15Pa`/`no차압`.
        버리고 있었다. PRES-002 가 이제 이걸 기준으로 삼는다. `no차압` 구간은 검사 안 한다
      - **기준값이 깨져서 잡은 버그**: LBL-001 3→4. 블록 재귀가 `SC2(기둥)` 안의 철골 규격
        (`H-350X350X12X19`)을 방 이름으로 빨아들였다. 방 3205 이름이 오염돼 있었다 → `label_exclude_blocks`
      - 신규 규칙 **PRES-004**(도면 내부 모순: 화살표 vs 절대압력). 엔진 자기점검 겸용
- [x] C1 칭량 대기실 오탐 해결 - **완료.** 봉쇄실 25 → 15 (10방이 오분류)
      - 한국어 합성명사는 **마지막 명사가 머리말**이다. `칭량 전 원료대기실` 의 머리말은 '대기실'
      - 전실, 대기실, 보관실, 기계실 = 그 공정을 하는 방이 아니다 / `과립액` = 액체다
      - 부정은 **분진 판정만 취소**한다 → `타정1실 전실` 은 중립이 아니라 **보호**(에어락은 압력 단계다)
- [x] C2 **ADJ-002 구현** - 예전 clause 가 "별표17 / PIC-S Annex 1 - **검수대기**" 였다.
      **조문 번호도 원문도 없이 규칙을 만들어 뒀던 것.** 법규 코퍼스에서 찾아 채웠다
      (별표1 제4호 가목: "에어락 역할을 하는 갱의실을 통해 이동해야 하며…")
      - 판정: 동선 그래프에서 **갱의실을 빼 본다.** 그래도 청정↔비청정이 이어지면 우회로가 있다
      - "갱의실이 있느냐"가 아니라 **"우회로가 없느냐"**. 뒷문 하나면 갱의실이 무력화된다
      - 과잉 검출 자가 발견: 10건 → **2건**(원인은 문 2개였다. 나머지는 중복)
- [x] C3 **신규 규칙 ADJ-003, ADJ-004** (별표17 제3.6호 부대구역)
      - ADJ-003 화장실 직결 금지 (나목: "화장실은 작업소 또는 보관소와 **직접 연결되지 아니하여야**")
      - ADJ-004 휴게실, 식당 분리 (가목: "휴게실과 식당은 다른 구역과 **분리**되어 있어야")
      - GMP 조문 대부분은 '가급적'으로 여지를 남기는데 **이 둘은 금지, 의무**다. 도면만으로 판정 가능
      - 실도면에 화장실이 0개 → **합성 데이터로 로직 완성**(원칙 ②). 데이터 오면 그날 켜진다
      - ADJ-004 한계를 규칙에 박음: **'분리 아님'은 증명해도 '분리 맞음'은 증명 못 한다**
        ('분리'는 별도 공조까지 요구 - 공조 계통도가 있어야 안다)
- [x] D1~D3 리포트, 검수 요청서
      - 리포트에 **압력 유형, 면적, 등급, 절대압력** 표 (유형이 틀리면 판정이 통째로 뒤집히므로 눈 검수용)
      - `docs/20_발주처_확인요청서.md` - 우리가 혼자 못 정하는 것 7가지
      - preview 가 이제 **"0건"의 이유**를 밝힌다 ("검사했고 위반 없음" vs "판정 대상 0")
- [x] E1~E3 마무리, 보고 - `docs/91_야간작업보고_2026-07-14.md`. 시험 160개. 기준값 불변
